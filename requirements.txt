# =============================================================================
# RIGELSLM - DEPENDÊNCIAS COMPLETAS (v1.0.0 - ESTÁVEL)
# =============================================================================
# Instale com: pip install -r requirements.txt
# Recomendado: usar python setup_env.py (que já aplica correções)
# =============================================================================

# --------------------------------------------
# 0. Ferramentas de build (evita erros de compilação)
# --------------------------------------------
wheel>=0.42.0
setuptools>=70.0.0

# --------------------------------------------
# 1. Núcleo (Sempre necessários)
# --------------------------------------------
torch>=2.0.0
tokenizers>=0.13.0                # Tokenização
transformers>=4.30.0              # (Opcional, usado em alguns utilitários)
numpy>=1.24.0                     # SEM TETO SUPERIOR – evita compilação de versões antigas
tqdm>=4.65.0                      # Barras de progresso
python-dotenv>=1.0.0              # Gerenciamento de .env
httpx>=0.24.0                     # Cliente HTTP para API e RSS
openai>=1.0.0                     # API DeepSeek (dialogos2.py)
langdetect>=1.0.9                 # Detecção de idioma (preparar_dados.py)

# --------------------------------------------
# 2. Processamento de Dados (Leitura de PDF, HTML, etc.)
# --------------------------------------------
pymupdf>=1.23.0                   # fitz – leitura de PDF
pdfplumber>=0.10.0                # Leitura de PDF (fallback)
beautifulsoup4>=4.12.0            # Parse de HTML
lxml>=4.9.0                       # Parser XML (usado com BeautifulSoup)
pytesseract>=0.3.10               # OCR (opcional)
pillow>=10.0.0                    # Imagens (para OCR)
requests>=2.31.0                  # Download de dados (download_datasets.py, RSS)
datasets>=2.12.0                  # Hugging Face datasets

# --------------------------------------------
# 3. Dashboard (FastAPI)
# --------------------------------------------
fastapi>=0.115.0
uvicorn>=0.30.6
jinja2>=3.1.4
python-multipart>=0.0.9
psutil>=5.9.0                     # Monitoramento de sistema

# --------------------------------------------
# 4. Conversão GGUF (Ollama, LM Studio)
# --------------------------------------------
gguf>=0.3.0                       # Geração de GGUF

# --------------------------------------------
# 5. Interface Web Alternativa (Gradio) – opcional
# --------------------------------------------
gradio>=4.0.0                     # app.py (se for usar)

# --------------------------------------------
# 6. Utilitários e Suporte
# --------------------------------------------
# (Nenhum adicional)